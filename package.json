{
  "name": "positions",
  "version": "0.3.7",
  "description": "View all available accounts and positions.",
  "main": "./lib/main",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "via",
    "positions"
  ],
  "repository": "https://github.com/via-platform/positions",
  "author": "Dakota Gordon",
  "engines": {
    "via": ">=0.8.0-alpha"
  },
  "dependencies": {
    "etch": "0.9.0",
    "underscore-plus": "^1.0.0",
    "via-table": "^0.2.4"
  },
  "deserializers": {
    "Positions": "deserialize"
  },
  "consumedServices": {
    "action-bar": {
      "versions": {
        "^0.1.0": "consumeActionBar"
      }
    }
},
  "providedServices": {},
  "configSchema": {}
}
